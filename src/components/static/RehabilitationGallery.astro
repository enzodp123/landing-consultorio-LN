---
import { Image } from "astro:assets";
import galeria1 from "../../assets/images/galeria-1.jpg";
import galeria2 from "../../assets/images/galeria-2.jpg";
import galeria3 from "../../assets/images/galeria-3.jpg";
---

<section id="galeria" class="py-16 md:py-24 bg-white overflow-hidden">
  <div class="w-[90%] lg:w-[75%] max-w-[1440px] mx-auto">
    <h2
      class="text-2xl md:text-3xl lg:text-4xl font-bold text-titulos mb-12 md:mb-16"
    >
      Rehabilitación pensada para <span class="text-primario"
        >tu cuerpo, tu ritmo y tu vida.</span
      >
    </h2>

    {/* Gallery Container */}
    <div class="relative w-full">
      {
        /* 
        Mobile & Desktop: Grid with 1 column on mobile, 3 columns on desktop 
      */
      }
      <div
        id="rehab-gallery-container"
        class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 w-full"
      >
        {/* Image 1 */}
        <div
          class="w-full relative rounded-2xl overflow-hidden aspect-[16/9] sm:aspect-[4/3] lg:aspect-[4/5] shadow-sm"
        >
          <Image
            src={galeria1}
            alt="Rehabilitación paciente 1"
            class="w-full h-full object-cover"
            width={600}
            height={750}
          />
        </div>

        {/* Image 2 */}
        <div
          class="w-full relative rounded-2xl overflow-hidden aspect-[16/9] sm:aspect-[4/3] lg:aspect-[4/5] shadow-sm"
        >
          <Image
            src={galeria2}
            alt="Rehabilitación paciente 2"
            class="w-full h-full object-cover"
            width={600}
            height={750}
          />
        </div>

        {/* Image 3 */}
        <div
          class="w-full relative rounded-2xl overflow-hidden aspect-[16/9] sm:aspect-[4/3] lg:aspect-[4/5] shadow-sm"
        >
          <Image
            src={galeria3}
            alt="Rehabilitación paciente 3"
            class="w-full h-full object-cover"
            width={600}
            height={750}
          />
        </div>
      </div>
    </div>
  </div>
</section>
